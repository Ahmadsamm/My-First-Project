const AboutUsPage = () => {
  // === DOM & VARS ===
  const DOM = {};

  // === INIT =========

  const init = () => {
    console.log('about');
  };

  // === EVENTHANDLER ===

  // ==== XHR ===========

  // === FUNCTIONS ====

  init();
};

export default AboutUsPage;
