const MenuMain = () => {
  // === DOM & VARS ===
  const DOM = {};

  // === INIT =========

  const init = () => {};

  // === EVENTS / XHR =======

  // === FUNCTIONS ====

  init();
};

export default MenuMain;
